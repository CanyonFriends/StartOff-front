import React from 'react';
import CommonHeader from '.';

export default {
  title: 'Layout/CommonHeader',
  component: CommonHeader,
};

export const commonHeader = (): React.ReactElement => {
  return <CommonHeader />;
};
