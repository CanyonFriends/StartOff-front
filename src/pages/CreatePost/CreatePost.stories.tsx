import React from 'react';
import CreatePostTemplate from './template';

export default {
  title: 'Template/CreatePost',
  component: CreatePostTemplate,
};

export const createPostTemplate = (): React.ReactElement => <CreatePostTemplate />;
